# Workflow Safety Execution Ledger

- premise: `.notes/oracle/cons-flow-ux/premise.md`
- assigned coverage: SAFE-001..SAFE-004, WF-001..WF-024 (28 tasks)
- consult slug: `cons-flow-ux`
- consult count: 1 / 1
- plan status: complete (`plan.md`, 28/28 task IDs covered)
- protocol/profile: v2 / `profile-b`
- execution status: C01–C12 complete (`SAFE-001`, `WF-001`, `WF-003`, `WF-002`, `WF-008`, `WF-004`, `WF-005`, `SAFE-003`, `WF-011`, `WF-006`, `WF-007`, `WF-009`, `WF-010`, `SAFE-004`, `WF-012`, `WF-017`, `WF-013`, `WF-014`, `WF-015`, `WF-016`, `SAFE-002`, `WF-018`, `WF-019`, `WF-020`, `WF-021`); next slice pending
- audit verdict: C01–C12 local audits PASS; whole-premise audit pending

## Receipts

- Bundle: 34 files, 887,556 bytes, non-empty, below 1 MiB.
- Prompt: prepared; exact size validated before submission.

## Step ledger

### C01 — SAFE-001 canonical sanitized preparation

- **Status:** Complete; ready for atomic commit `fix(ai-context): unify sanitized message preparation [SAFE-001]`.
- **Decision branch:** The protocol had no typed context-unavailable fact. Added `InputFailure::ContextUnavailable` and `AiFailureCode::ContextUnavailable`, with typed classification, safe presentation, immediate/manual-retry planning, exhaustive model coverage, and raw detail confined to the diagnostic vault.
- **Implementation:** Routed all six `AiContextPart` variants through `resolve_context_part_sanitized`; added recursive JSON `base64Data` stripping regardless of MIME, character-safe truncation, XML attribute escaping, explicit primary/supplemental roles, private non-serializable `PreparedUserMessage`, and privacy-safe v2 receipts. Preserved unreadable-existing-file metadata fallback and Flow-specific skill wording/wrapping. Migrated Agent Chat, prompt handoff, preflight audit, storage, protocol input, and runtime reconstruction callers.
- **Compatibility:** Legacy receipt loading ignores v1 content-bearing fields. Audit schema v3 persists character counts rather than raw authored/final content, and startup migration clears legacy v1/v2 audit content columns.
- **Self-audit correction:** The first Agent Chat migration crossed the compatibility string boundary and reconstructed a failure from safe prose. Added the crate-private typed resolver and migrated Agent Chat to carry the original `AppFailureRecord`. Also restored Flow wording and `<flow>` tags, escaped focused-label metadata, extended exhaustive protocol/presentation matrices, and asserted legacy database columns directly before final proof.
- **Focused receipts:** `ai::message_parts` 40/40 PASS; `ai::reliability` 28/28 PASS; `ai::preflight_audit` 4/4 PASS; storage round-trip 1/1 PASS; `ai::agent_prompt_handoff` 21/21 PASS; redacted probe wire contract 1/1 PASS; `sk-protocol` 24/24 PASS; library check PASS.
- **Build:** `SCRIPT_KIT_AGENT_ARTIFACT_NAME=cons-flow-safe001 ./scripts/agentic/agent-cargo.sh build --bin script-kit-gpui` PASS. Stable artifact: `target-agent/artifacts/cons-flow-safe001/script-kit-gpui`; SHA-256 `c11299fd6d0a1e2c887eb028ca52b7635046ebc683ed765c07ffa1f3e1765745`.
- **Runtime receipt:** `.artifacts/consistency/cons-flow-ux/safe001-canonical-v2/SAFE-001/receipt.json` → `RUNTIME-CONFIRMED`. Oversized nested JSON: Ready, binary stripped, nonbinary preserved, bounded 335-character payload. Missing primary: Blocked and `acceptedSend:false`. Missing supplemental with authored text: Partial and `acceptedSend:true`. Safe user copy exact; raw content/source canaries absent from serialized receipts and visible Agent Chat semantics.
- **Cleanup:** Driver `try/finally` receipt reports `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, exact artifact-path `ownedProcessCount:0`, and final owned-process count zero. Clipboard untouched; no signal used.
- **Governance:** Source-audit inventory remains 2,818 reader sites with no new guarded reader. Hardcoded-visual inventory has no additions. `git diff --check` passes. Protected glass diff is empty. Test-only inspection route rejects fixtures outside `SCRIPT_KIT_TEST_STATUS=1`.
- **Adversarial audit:** PASS. Every variant reaches the canonical sanitizer; no production model-bound caller bypass was found; Agent Chat preserves the original typed failure; receipts and `Debug` output cannot serialize model content; legacy content is discarded/redacted; Flow behavior is preserved; failed primary content cannot send; and runtime-owned processes are gone.

### C02 — WF-001 and WF-003 accepted-send lifecycle

- **Status:** Complete; ready for atomic commit `feat(ai-context): model staged provenance and retry lifetime [WF-001 WF-003]`.
- **Implementation:** Added `StagedContextItem` with stable ID, generation, canonical identity, explicit provenance/role/state/lifetime/removability, in-place priority upgrades, and redacted `Debug`. Migrated active, queued, draft, portal, host-handoff, export, and protocol carriers to typed items. Schema v7 exposes only safe lifecycle facts and separates immutable receipts from pending context.
- **Ingress decision:** Every production entry now names provenance. Deliberate inline/spine/slash selection is `UserMention`; attachment/history portals are `AttachmentPortal`; explicit cross-surface transfer is `HostHandoff`; launcher focus is `ImplicitFocused`; ambient capture is `DeferredAmbient`. Primary wins; equal-role priority follows the Oracle plan.
- **Accepted-send decision:** Background preparation leaves composer/transcript/context intact. Primary failure, capability refusal, adapter refusal, and reliability-start refusal restore exact snapshots. Accepted runtime start alone commits resolved items to immutable `ThreadReceipt` records. Transition-owned item snapshots cover queued turns even when their chips are no longer in the active composer collection.
- **Retry decision:** `last_prepared_turn` stores the accepted blocks/attachments/display text and a run-scoped fingerprint. Retry and recovered-resume submit those stored blocks directly; current files, focused target, ambient capture, and pending chips are not re-resolved.
- **Portal decision:** Portal sessions snapshot typed pending items plus consumption state; Escape restores draft text, cursor/selection, picker state, exact IDs/generations, and focus.
- **Focused receipts:** `ai::staged_context` 4/4 PASS; thread 92/92 PASS; full Agent Chat 630/630 PASS; Agent Chat state 51/51 PASS; wait/probe protocol 46/46 PASS; message parts 40/40 PASS; Quick Question suppression 1/1 PASS; embedded draft fixture 1/1 PASS; library check PASS.
- **Build:** `SCRIPT_KIT_AGENT_ARTIFACT_NAME=cons-flow-c02 ./scripts/agentic/agent-cargo.sh build --bin script-kit-gpui` PASS. Stable artifact SHA-256 `6c5152045e079e239d46eba4cfa92088fb3631e301fef94017fa9bb0c33c8486`.
- **Runtime receipts:** `.artifacts/consistency/cons-flow-ux/c02-context-lifecycle-v1/{WF-001,WF-003}/receipt.json` → `RUNTIME-CONFIRMED`. Dedupe: one upgraded pending item. Partial accepted: one immutable primary receipt plus one failed supplemental and non-empty immutable payload fingerprint. Fresh thread: zero pending/receipt/payload. Portal Escape: exact draft/cursor/selection/picker/item ID/generation/focus restored.
- **Cleanup:** `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, exact artifact-path `ownedProcessCount:0`, clipboard untouched, no signal.
- **Escalations:** The first current-tree bin test exposed a stale `pending_context_parts` draft fixture in `src/app_impl/actions_dialog.rs`; migrated it to a typed staged item and proved the owning test. The first runtime probe failed closed because `openAgentChatKitchenSinkFixture` was deleted in the current tree; switched only the opening command to the supported `openAiWithMockData` fixture and reran the same assertions. An adversarial pass found capability refusal could return before restoring `Resolving` state and queued transitions depended on the active composer collection; both were corrected and locked by transition-snapshot tests.
- **Governance:** Source-audit inventory remains 2,818 sites with no guarded additions. Hardcoded-visual inventory has no additions. Receipt canary scan passes. `git diff --check` passes. Protected glass owner diff is empty. Exact process inventory is zero.
- **Adversarial audit:** PASS. No production pending carrier stores a raw `Vec<AiContextPart>`; preparation-only vectors are ephemeral resolver inputs. All ingress provenance is explicit. Failed primary cannot reach adapter start. Every pre-accept exit restores state. Accepted start alone commits receipts. Failed supplementals remain pending. Retry uses one immutable payload. Quick Question stays empty. Portal cancellation restores the full snapshot. Saved-message reload clears pending, receipt, and retry state; the persisted history schema has no legacy pending-context field to resurrect. Protocol/runtime receipts contain no raw path, URI, context body, or failure detail.

### C03 — WF-002 and WF-008 truthful entry outcomes and verbs

- **Status:** Complete; ready for atomic commit `fix(agent-chat-entry): report outcomes and enforce entry verbs [WF-002 WF-008]`.
- **Typed entry decision:** Replaced `seed_text + AgentChatSeedPolicy` with `AgentChatEntryIntent::{Open,Add,Continue,Ask,Send}` at both request and `TabAiLaunchRequest` boundaries. `Open`/`Add`/`Continue` cannot submit; `Ask`/`Send` reject empty text. Production callers are enumerated in `c03-entry-caller-contract.md`; repository search finds zero `AgentChatSeedPolicy`/`seed_policy` references.
- **Outcome decision:** `AgentChatEntryDispatch` is must-use. `Complete` carries a redacted outcome; `Pending` carries a non-cloneable ticket with one receiver. The outcome reports request ID, verb, blocked/reused/fresh disposition, destination host/thread/generation, text/context staging, submission state, blocked reason, and return route. Fire-and-forget callers attach the observer. Host/thread identity gates message-count acceptance, so unrelated thread activity cannot satisfy a request.
- **Context decision:** Added `AgentChatContextPolicy::NoContext` for Quick Question and Quick AI. Unlike `SuppressFocused`, it also suppresses standing instruction notes and every ambient/implicit source. A real runtime red pass found Quick Question still staged one `#instructions` note under `SuppressFocused`; `NoContext` corrected the owning launch/staging boundary, and the rerun reports zero context chips.
- **Caller migration:** Generic launcher text is Open/draft-only. Quick AI query and plain Cmd+Enter are Ask. Dictation delivery and capture/quick-submit plans are Send. Notes, Actions, selected rows, plugin skills, composer dictation, and explicit parts are Add. Quick AI promotion is Continue. Plugin-skill Add keeps specialized slash-selection identity, cursor, and thread affinity through a dedicated policy branch.
- **Preflight decision:** Profile/model resolution, sidecar warm/cold acquisition, and zero-context admission occur before `AppView` replacement. Failure now preserves the source instead of installing a setup view. A test-status-only deterministic refusal fixture proves view and exact draft preservation without changing production policy.
- **Focused receipts:** `quick_question_contract` 10/10 PASS; entry choke point 3/3 PASS; plugin skill 4/4 PASS; portal dictation 7/7 PASS; focused migrated dictation, Cmd+Enter, Brain Inbox, script-issues, and dictation-owner tests PASS; binary check PASS; product build PASS.
- **Build:** `target-agent/artifacts/cons-flow-c03/script-kit-gpui`; SHA-256 `9cfcf16b580b3fca23e2965e2e0ec5e3df260291f51f2c00e317ed9a734f5777`.
- **Runtime receipts:** `.artifacts/consistency/cons-flow-ux/c03-entry-verbs-v1/{WF-002,WF-008}/receipt.json` → `RUNTIME-CONFIRMED`. Open stages a draft and produces zero messages; Ask produces exactly one submitted turn; Quick Question is empty with zero context and zero messages; preflight refusal preserves the source and 34-character draft. Receipt serialization contains no authored text.
- **Cleanup:** Four Driver runs each report `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, exact artifact-path `ownedProcessCount:0`, and `clipboardTouched:false`; no process signal.
- **Governance:** Source-audit inventory remains 2,818 sites. Hardcoded-visual tests PASS 16/16. `git diff --check` PASS. Protected glass owner diff is empty; glass Bun contracts PASS 40/40 and measured calibration fixture PASS 1/1.
- **Escalations:** Initial runtime proof exposed the standing instruction-note leak described above. The preflight negative control initially failed because the sandbox profile's explicit Pi path correctly outranked the environment override; added a test-status-only preflight refusal seam rather than weakening profile resolution. Broad source-audit suites also surfaced failures in unrelated dictation availability, submit ordering, footer refresh/slot, and Spine assertions outside the changed entry paths; focused changed-owner assertions are green and no unrelated source contract was patched to claim full-suite success.
- **Adversarial audit:** PASS. Open cannot map to Sent; Ask/Send are the sole submitting variants; empty explicit submissions are rejected; Quick Question carries no context; failed preflight leaves source state intact; per-thread identity prevents unrelated acceptance; Notes pending results fail closed; plugin skill semantics survive migration; receipts contain no raw text; runtime-owned processes are gone.

### C04 — WF-004 and WF-005 semantic roles and executable descriptors

- **Status:** Complete; ready for atomic commit `refactor(conversations): make semantic roles and commands executable [WF-004 WF-005]`.
- **Semantic decision:** Added the exact protocol roles `contextChip`, `identityBadge`, and `destinationSelector`, plus the role-safe actions `removeContext`, `openContextDetails`, `openIdentitySelector`, `openIdentityDetails`, and `selectDestination`. Agent Chat pending/receipt context, Agent Chat profile/model identity, Flow identity, Notes/Today document identity, and Dictation destination selection all project from the typed state their renderers consume. Removable context exposes separate details and remove regions; identity cannot acquire remove semantics.
- **Command decision:** `BoundConversationCommand<Handler>` couples every descriptor to a host-owned executable token. Closed IDs, labels, shortcuts, role, typed availability, safe disabled reasons, confirmation policy, and semantic action IDs are shared by footer, Actions, key routing, execution, and `getElements`. Agent Chat, Flow, and ordinary ChatPrompt retain separate handler enums and state. Unsupported ChatPrompt Background/New commands are absent; supported-but-unavailable commands remain visible with typed safe reasons; destructive Flow termination is confirmation-required.
- **Host behavior:** Agent Chat Send/Stop/Retry/New/Copy route through one executor in embedded, Notes-hosted, and detached contexts. Flow uses the descriptor grammar `↵ Send · ⌘K Actions · Esc Background` or `⌘. Stop · ⌘K Actions · Esc Background`. ChatPrompt leaves plain `⌘C` to native selection copy and uses `⇧⌘C` for Copy Last. Dictation chip clicks only retarget during Recording/Confirming; Option and armed state never turn selection into delivery. Notes/Today identity is privacy-safe. A runtime-discovered Day Page entity double lease was removed by deriving footer preview facts from the current entity and passing them to a pure footer builder.
- **Automation seam:** Added test-status-only `openChatPromptFixture`, routed through the same `PromptMessage::ShowChat` path as SDK `chat()`, so the Driver can inspect an ordinary ChatPrompt without a script subprocess taking over its control channel. The probe waits for startup `show` to settle before opening the fixture, preventing a delayed startup reset.
- **Focused receipts:** `components::conversation_actions` 9/9 PASS; `sk-protocol` 24/24 PASS; requested `app_layout` invocation PASS with zero matching tests (runtime elements are the substantive projection proof); `prompts::chat` 72/72 PASS; `ai::agent_chat` 630/630 PASS; `flows::` 125/125 PASS; `dictation::` 276/276 PASS; `actions::builders` 226/226 PASS; `stdin_commands` 69/69 PASS; Flow footer binary target 12/12 PASS; disabled New Conversation binary target 1/1 PASS.
- **Build/runtime receipt:** Stable artifact `target-agent/artifacts/cons-flow-c04/script-kit-gpui`, SHA-256 `3ced6969f86f1eecea981774ea4f7a88879581d240d4a025e5391ee79c5e24d9`. `.artifacts/consistency/cons-flow-ux/c04-semantic-commands-v1/{WF-004,WF-005}/receipt.json` → `RUNTIME-CONFIRMED`; receipt SHA-256 `fe8fa1beade506ae7584c5bd416f2b2228b9af91d9f1dc4630cbd126d0233fcb`. Agent Chat, Flow, Notes, Today, Dictation, and ordinary ChatPrompt all pass role/action/command uniqueness, safe disabled-copy, confirmation, and unsupported-command assertions.
- **Cleanup:** Six isolated Driver scenarios each use `try/finally` and `driver.close()`. Every row reports `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, exact artifact-path `ownedProcessCount:0`, and `clipboardTouched:false`; no signal or broad process match was used.
- **Governance:** Source-audit inventory remains 2,818 reader sites and is current; source-audit plus hardcoded-visual governance tests pass 34/34; hardcoded-visual inventory has no additions relative to C04 HEAD; `git diff --check` passes. Protected calibration owners/envelopes have no diff; glass static contracts pass 40/40 and the measured production fixture passes 1/1.
- **Escalations:** Removed inherited Flow ChatPrompt command elements before appending Flow descriptors to eliminate duplicate `conversation.send`. Repaired the Day Page render-time self-read described above. Replaced a stale Agent Chat fixture command with the supported launcher action. Added the ordinary ChatPrompt fixture after a real script-run proved the script protocol owned the Driver channel. Serialized startup show before fixture open after a runtime log proved the delayed show reset ChatPrompt.
- **Adversarial audit:** PASS. Exact roles/actions are closed and role-safe; every projected command carries a handler token; descriptor validation rejects duplicate IDs/semantic IDs, destructive-without-confirmation, and blank disabled reasons; shortcut routes consume the same descriptor lists; renderer/Actions/automation paths derive from the same host adapters; unsupported ChatPrompt commands are absent; Flow lifecycle remains Flow-owned; context and identity mutations remain separate; Dictation destination inspection/selection cannot deliver; all six runtime hosts are unique and clean; no locked calibration value changed.

### C05 — SAFE-003 archive/history safety and WF-011 truthful Flow identity

- **Status:** Complete; local adversarial audit PASS; ready for atomic commit `fix(flow): archive history and expose truthful session identity [SAFE-003 WF-011]`.
- **Consult:** `flow-c05-archive`, exactly 1/1, completed successfully through round-robin browser profile-b. No follow-up consult.
- **Implementation:** Canonical v4 active/archive manifests migrate v0–v3 without turn loss, distinguish present empty metadata from absence, retain every turn, repair malformed v4, and use monotonic revisions plus selected-thread tombstones. New archives current state; archives are retained/read-only; Continue clones with lineage; Terminate is confirmed Actions-only runtime forget; confirmed Delete removes only selected; dismissal paths never delete. Flow identity separates engine/model, summarizes cwd/origin safely, and reports selection/lineage/retention/rethread truth. Seven typed desk states and one selected-row descriptor drive render, state/elements, Actions, both footers, Enter, and Shift+Enter.
- **Escalations:** Replaced the 50 ms explain test with one injected absolute-deadline seam and retained a separate owned-process test. Runtime found that fast mdflow could reach Succeeded before a Running observation; terminal acceptance now clears rethread. The first probe assumed an in-window `confirmPrompt`; it now targets the real `confirm-popup` automation window. A second rapid confirmation exposed expected Actions-parent focus restoration being mistaken for a parent click; destructive Flow actions acknowledge the requested parent activation before opening the child popup, with no hardcoded delay.
- **Focused proof:** Flow session model 55 PASS/1 ignored; conversation actions 10/10; Flow Session binary 32/32; Flow Desk binary 10/10; sk-protocol reliability 15/15; `check --lib` PASS. Exact 20/1,000-turn tests pass. Two fresh-process combined serde medians are 1.837 ms and 1.815 ms, retaining one atomic manifest.
- **Build/runtime:** `target-agent/artifacts/cons-flow-c05/script-kit-gpui`, SHA-256 `1bded18b685837831f5579ba3fcf4692c22908ae6f75c27633e491fd69398ece`. `.artifacts/consistency/cons-flow-ux/c05-flow-archive-v1/runtime/flow-history-receipt.json` passes nine scenarios: 15-turn seed/restart, New/archive/Continue, idle/active Terminate, Delete cancel/confirm, stale-write release, dismissal preservation, and all seven desk states.
- **Cleanup/privacy:** Nine Driver rows each report `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, `ownedProcessCount:0`, `fixtureOwnedProcessCount:0`, no forced termination, and unchanged clipboard change count. All five privacy counters are zero.
- **Governance:** Source-audit inventory reports no new guarded reader sites relative to `d4287ef3a`; no executable lane-governance script exists, so focused reliability/source/privacy/hardcoded-visual/glass gates are authoritative. Protected owner diff is empty; Bun glass suite 40/40 and production calibration fixture 1/1 pass. No surface-local visual values were added.
- **Forward progress index:** 18 = prior 10 +2 product/model complete +2 focused/current-byte proof +2 stable artifact/runtime +1 governance/privacy/glass +1 docs/audit/commit readiness.
- **Lifecycle:** Local commit authorized; push, deploy, tag, release, publication, and `.hitl-align` access remain unauthorized and were not performed.

### C07 — SAFE-004 Notes executable descriptors

- **Status:** Complete; local adversarial audit PASS; ready for atomic commit `fix(notes): make action descriptors own shortcuts and execution [SAFE-004]`.
- **Implementation:** Added closed Notes surface/context, availability, confirmation, and descriptor models. One mode-sensitive list now owns IDs, labels, shortcuts, semantic IDs, destructive policy, Actions rows, titlebar controls, live key lookup, target-scoped automation, and redacted runtime projection. One exhaustive `handle_action` owns effects. Shared key normalization recognizes GPUI bracket aliases. Format is Actions-only; Open Trash is Shift+Command+T; Delete is Shift+Command+Backspace only; Ask AI is Command+Enter; Shift+Command+A and Shift+Command+Delete are unowned.
- **Focus correction:** Runtime proof found closing Actions while preview was rendered focused the hidden editor. Added `NotesFocusSurface::Preview` and a pure primary-focus decision so Actions/dialog/delete restoration targets the rendered editor or preview root. Model test PASS; repeated real preview shortcut activation PASS.
- **Automation correction:** Runtime proof found target-scoped `simulateKey` hardcoded only a small Notes subset and bypassed descriptor execution. Expanded the existing agent-facing seam to attempt the current descriptor lookup for every Notes-targeted key after modal/host-chrome precedence. Command+P, Command+Enter, Restore, Find, and all other advertised chords now share `handle_action`; Command+K remains host chrome.
- **Semantic correction:** The Notes collector statically advertised obsolete `button:notes-ask-ai`, even in modes where the renderer omitted it. `getElements` now consumes live titlebar descriptors and exposes the same semantic IDs, labels, shortcuts, and availability as rendering. Ask AI, Restore, and Delete Permanently are runtime-proven.
- **Focused proof:** Shortcut normalization 5/5 PASS; Notes action model 11/11 PASS; Notes Actions builder 13/13 PASS; Notes window/focus 99/99 PASS; `check --lib`, `check --bin script-kit-gpui`, and stable build PASS. Stable artifact SHA-256 `e82c143095f3a102e1e62b0556b5c689f0b9a92caf62b3fbfcb078c61a0a0196`.
- **Runtime:** `.test-output/cons-flow-c07/notes-actions-receipt.json` PASS. Six projections cover editor-selected, preview-selected, Trash-empty, read-only, editor-no-current, and Trash-selected. Every descriptor matches one Actions semantic row; all 18 unique advertised shortcuts execute; exact generation increments prove one semantic action per activation. Delete, permanent delete, and empty trash each confirm before mutation and cancel with counts unchanged.
- **Cleanup/privacy:** Driver uses a sandbox Notes DB and exact NSPasteboard archive under `try/finally`. Final receipt: `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, `ownedProcessCount:0`, `forcedSignals:[]`, `clipboardRestored:true`, `databaseRemoved:true`. Receipt contains no note text, URI, temporary path, or artifact path. Structured action logs contain only action ID, semantic ID, and generation; delete-confirmation logs use title length/fingerprint rather than title text.
- **Governance:** Source inventory remains 2,818 sites with no new guarded reader relative to C06. Python governance 34/34 PASS. Hardcoded visual inventory has no additions. Protected owner diff is empty; Bun glass contracts 40/40 PASS and production calibration fixture 1/1 PASS. `git diff --check` PASS.
- **Source-audit pruning:** Removed brittle keyboard source-text assertions superseded by typed descriptor/normalization tests and the six-mode runtime matrix. No test was ignored, no threshold changed, and no formatted source audit was added.
- **Adversarial audit:** PASS. Catalog coverage is nonzero in applicable modes; per-mode IDs/semantics/normalized shortcuts are unique; every enabled advertised shortcut resolves to exactly one descriptor; the exhaustive enum match supplies every handler; unsupported actions are absent; destructive flags imply confirmation; pointer/Actions/GPUI/automation converge on `handle_action`; titlebar semantics derive from current descriptors; negative aliases do nothing; read-only/no-current policies are explicit; runtime cleanup is exact; protected visual/motion owners are unchanged.
- **Lifecycle:** Local commit authorized. No push, deploy, tag, release, publication, credential access, or `.hitl-align` inspection performed.

### C08 — WF-012 shared Notes search and WF-017 truthful host destinations

- **Status:** Complete; local adversarial audit PASS; ready for atomic commit `fix(notes-search): share results and truthful host destinations [WF-012 WF-017]`.
- **Canonical search decision:** Added one typed regular/day document model, stable IDs, one rank/tie-break path, stable automation metadata, typed destination policy, typed Loading/Failed/ReadyEmpty/Ready state, and host state preserving selected ID plus scroll anchor across refresh. Strict day-note IO failures cannot become empty results; loading/failure retain prior rows.
- **Host decision:** Notes Window uses `Open in Notes`; Day Page uses `Open Here`; standalone Browse uses `Open in Notes Window`; Agent Chat portal uses `Attach Note`. Standalone and portal Enter, second click, and native footer converge on one destination-aware row activation owner. Portal Escape restores draft/query, cursor, context, selected-ID evidence, and composer focus.
- **Runtime defects repaired:** Day Page used a test-isolated days directory rather than the canonical Notes brain directory; it now shares the corpus. Popup failure replaced prior rows; it now appends disabled recovery status after retained rows. Relative-time descriptions made cross-host metadata drift across minute boundaries; stable machine metadata is separate. Empty result inspection exposed three eager `item_count - 1` underflows through `then_some`; all use saturating bounds. Notes failure copy no longer leaks AI-context-specific prose. Portal proof now asserts the truthful `attachmentPortal` note part instead of a misleading total-chip increase.
- **Focused proof:** Canonical model 5/5 PASS; Notes Actions builders 15/15 PASS; native footer contract 1/1 PASS; library check PASS; binary check PASS; source-audit inventory regenerated; `python3 -B tests/source_audit_inventory.py --base main` PASS with no new guarded reader sites.
- **Build/runtime:** `target-agent/artifacts/cons-flow-c08/script-kit-gpui`; SHA-256 `03d413be22cc9390bd47ee499aee0ef57d3088c45abcc48562d147145a25c774`. `.test-output/cons-flow-c08/notes-search-receipt.json` → PASS. Four hosts expose identical ordered IDs/titles/kinds/metadata. NoMatch, stable selection, Loading with prior rows, Failed with prior rows, ReadyEmpty, standalone Enter/second click, portal Enter/second click, and portal cancellation restoration all pass. Runtime panic count is zero.
- **Cleanup/privacy:** Two Driver sessions report `processExited:true`, `streamsDrained:true`, `logWriterClosed:true`, `ownedProcessCount:0`, `forcedSignals:[]`, and `databaseRemoved:true`. Post-probe exact executable inventory also reports zero owned processes. Stable semantics use fingerprints/lengths and do not expose note bodies or raw failures.
- **Glass/governance:** Protected owners and calibration values are unchanged. Bun glass contracts PASS 40/40; production calibration fixture PASS 1/1; Actions-entry filmstrip PASS; rapid-toggle stress PASS. Full lifecycle capture remains EVALUABLE_FAIL only for under-resolved main-entry geometry and missing Notes pre-reveal frames. A control run against the prior C07 artifact reproduces the exact same scenario pass/fail vector and predicates (`main-entry`: 6 frames/2 widths/first-width miss; `notes-entry`: no hidden sample, zero pre-visible frames, body mask false), while main-exit, close-before-settle/reopen, dictation, and interference pass on both. Classified as matched pre-existing capture-resolution limitation, not C08 regression; no locked fixture, threshold, or motion value changed.
- **Source-audit pruning:** Removed the obsolete `notes_browse_surface_contract.rs` and `notes_browse_text_state_contract.rs` formatted-source audits after canonical model tests and the four-host runtime matrix superseded their old local/index-based ownership assumptions. Inventory now reports 2,816 reader sites, and the ordinary-PR base guard passes.
- **Adversarial audit:** PASS. All four hosts share canonical IDs/order/metadata; regular/day identity is retained; errors cannot become empty; prior rows survive loading/failure; selection and anchor reconcile by ID; exact destination verbs match exact effects; standalone and portal keyboard/pointer routes converge; portal cancellation restores the complete parent snapshot; semantics derive from renderer state; runtime cleanup is exact; no protected calibration owner changed.
- **Decision branch:** Lifecycle C08 rerun repeated the initial capture failure with no interference. The prior C07 artifact then reproduced the same exact failure predicates, selecting the Oracle-plan non-regression branch: retain fail-closed receipts, require the other protected runtime gates to pass, record the matched control, and do not alter unrelated locked calibration/harness thresholds inside C08.
- **Lifecycle:** Local commit authorized. No push, deploy, tag, release, publication, credential access, or `.hitl-align` inspection performed.

### C09 — WF-013 shared mention grammar, WF-014 exact AI scope, and WF-015 host restoration

- **Status:** Complete; local adversarial audit PASS; ready for atomic commit `fix(notes-today): align mentions ai scope and host restoration [WF-013 WF-014 WF-015]`.
- **Implementation:** Notes adopts a local context-mention picker while Today retains its main-menu round trip. Both use shared Spine row IDs, insertion references, aliases, and atomic-delete logic. A closed `NotesAiScope` distinguishes `WholeNote`, `Selection`, `CurrentLine`, `Resource`, and `AttachedNote`; Notes stages its whole live editor snapshot, Today stages only a real selection/current line unless whole Today is chosen explicitly, and Add/Attach/Ask-without-question remain composer-only. Notes and Today carry host-owned, privacy-safe return snapshots with exact instance/entity and generation validation, preserve live unsaved state, and fail closed on stale callbacks.
- **Escalations:** Model-level Markdown-reference deletion did not initially reach Notes runtime; wired the shared atomic fixup into Notes `on_editor_change` and retained complete registered references through adjacent edits. Notes Command+Enter required the target-aware legacy automation seam that invokes the same typed action descriptor. Structured restore logging required polling the asynchronous log writer. Today selection required target-scoped real GPUI Shift+Left. Repeated cached Agent Chat observations were unavailable after the first crossing, so the first current-line chip observation proves the integration boundary while selection/whole-note runtime owner receipts prove exact later scopes. The source-audit base guard rejected one added `function_body` reader; moved the invariant back to the existing ordinary-footer reader and relied on typed/runtime preview coverage rather than grandfathering formatted source.
- **Focused proof:** Notes editor 42/42; Notes AI scope 2/2; Notes host-return 8/8; Agent Chat entry 10/10; Day Page binary 20/20; focused Day Page source audits 9/9. `python3 -B tests/source_audit_inventory.py --base main` PASS with 2,814 reader sites and no guarded additions. Hardcoded visual inventory PASS 16/16; Bun glass contracts PASS 40/40; production calibration fixture PASS 1/1; `git diff --check` PASS.
- **Build/runtime:** `target-agent/artifacts/cons-flow-c09/script-kit-gpui`, SHA-256 `483dee3182df9ebbb8154bff062b55208547a6b74043fe4e5d7cf30fd3942dac`. `.test-output/cons-flow-c09/notes-today-receipt.json` passes all four scenarios: Notes local mention/insertion/atomic deletion/loading, Notes whole-live-snapshot Agent Chat return, Today current-line/selection/whole-page scope matrix, and Today Agent Chat return.
- **Cleanup/privacy:** Every Driver child reports `processExited:true`, `streamsDrained:true`, and `logWriterClosed:true`. The combined exact artifact-path inventory reports `exactArtifactOwnedProcessCount:0`. Handoff and host-return receipts expose only lengths, run fingerprints, IDs/generations, counts, scope, destination, and staging outcome; synthetic content and raw URIs are absent.
- **Adversarial audit:** PASS. Notes and Today use the required distinct navigation hosts but shared grammar/results/tokens; complete aliases survive adjacent edits and delete atomically; `-` grammar was not changed; loading states remain visible; the scope enum is closed; range canaries are excluded; all Add/Attach/default Ask paths are composer-only; Notes uses its live unsaved editor; Today preserves its held entity; Notes remains open during main Agent Chat; exact focus/content/selection/mode state returns; stale instance/window/focus/entity generations fail closed; protected motion owners remain unchanged; and all runtime-owned processes are gone.
- **Decision branch:** The source-audit inventory rejected the newly added second `function_body` read of `day_page_view.rs`. Repository policy selected the higher-rung branch: remove that formatted preview-helper inspection, retain the existing ordinary-footer assertion, and use the real Day Page scope/runtime matrix for preview-specific behavior. Inventory then passed without a new reader or weakened runtime negative control.
- **Lifecycle:** Local commit authorized. No push, deploy, tag, release, publication, credential access, or `.hitl-align` inspection performed.

### C10 — WF-016 transactional Notes cart handoff

- **Status:** Complete; local adversarial audit PASS; ready for atomic commit `fix(notes-agent-chat): consume cart items transactionally [WF-016]`.
- **Implementation:** `NotesAiHandoffPayload` now materializes a primary `StagedContextItem`, one `NotesHandoffAttachment` per cart row, stable request/idempotency identities, and the existing generation-safe return snapshot before touching main-window state. The binary hook returns typed primary and per-item accepted/duplicate/failed outcomes instead of `Result<bool, String>`. Agent Chat admits primary first, stages supplementals independently, reuses only a live Standard full-session main chat, preserves its draft/thread/context/messages, never selects detached chat, and shows main only after primary success. Notes validates request/mapping identity and deletes only accepted/duplicate rows; failed, unreported, mismatched, and delete-failed rows remain.
- **Escalations:** The first runtime attempt exposed the existing re-entrant main-window update boundary: the cross-window hook returned before staging and the new typed caller initially lost the old string-based retry classification. Added `NotesAiMainHandoffFailure` with exact `MainWindowUnavailable` versus `MainStagingFailed` kinds, then retried the same immutable payload once; the rerun logs `notes_ai_handoff_retry_completed`. The duplicate scenario initially left main Agent Chat before retrying, so it correctly opened a new main thread and accepted the attachment. The proof was corrected to invoke Notes again while the suitable main chat was still live, which exercised the intended reuse contract and produced a real `duplicate` outcome without changing the draft/thread/context count.
- **Focused proof:** Notes handoff 10/10 PASS; staged-context provenance/dedupe 4/4 PASS; library check PASS; binary check/build PASS. Source-audit base guard PASS with 2,814 sites and no guarded additions. Hardcoded visual inventory 16/16 PASS. Protected glass contracts 40/40 and production calibration fixture 1/1 PASS. `git diff --check` PASS.
- **Build/runtime:** `target-agent/artifacts/cons-flow-c10/script-kit-gpui`, SHA-256 `99243f05e6e3cb6b5bdca9d8e396095e6786006d95f0f2e396e177351230665c`. `.test-output/cons-flow-c10/notes-handoff-receipt.json` passes three scenarios: accepted+failed partial staging followed by canonical duplicate reuse, primary-failure atomicity, and post-staging cart-delete failure.
- **Cleanup/privacy:** All three Drivers report `processExited:true`, `streamsDrained:true`, and `logWriterClosed:true`; each sandbox database is removed; exact artifact inventory reports `exactArtifactOwnedProcessCount:0`. Receipt scans find no synthetic note/attachment body or raw `test://` URI. Attachment `Debug` exposes only cart-ID length, redacted staged-item facts, and idempotency fingerprint.
- **Adversarial audit:** PASS. Payload materializes before main mutation; request-ID mismatch fails closed; forged/missing per-item mappings cannot consume; primary failure stages no supplements and consumes nothing; accepted/duplicate consume only their rows; failed rows remain; delete failure retains the row with truthful feedback; bounded retry reuses the same payload; first shown main state has context and zero messages; suitable main chat preserves draft/thread/context; unsuitable main host opens embedded Standard; detached state is unchanged; Notes stays open; and no protected calibration owner changed.
- **Decision branch:** Current context admission already returns canonical `Added`/`Upgraded`/`Duplicate` with a stable winning item ID, so C10 extended that owner with prebuilt-item staging instead of inventing a second dedupe implementation. Runtime fixture-only refusals and cart-delete failures are guarded by `SCRIPT_KIT_TEST_STATUS=1`; production policy remains unchanged.
- **Lifecycle:** Local commit authorized. No push, deploy, tag, release, publication, credential access, or `.hitl-align` inspection performed.

### C11 — SAFE-002 safe dismissal, WF-018 canonical targets, and WF-019 selection-only chips

- **Status:** Complete; local adversarial audit PASS; included in atomic commit `fix(dictation): unify targets chips and safe dismissal [SAFE-002 WF-018 WF-019]`.
- **Target decision:** Added one exhaustive eight-row `DictationTargetDescriptor` table owning stable ID, selector/badge labels, icon, delivery verb, description, persistence, frozen-identity requirement, auto-submit permission, recovery capabilities, quick-chip order, and selectability. Seven current targets project into a real Command+K/footer Destinations Actions menu; four project into the compact chip row. `AiChatComposer` remains deserializable/exhaustive but is read-only legacy state; old aliases migrate to `TabAiHarness` with `migrated_legacy_ai_chat:true`.
- **Actions correction:** Initial C11 automation exposed seven descriptor rows but did not satisfy the plan's user-visible “Open Dictation Actions” clause. Added a shared `CommandBar` host to the overlay, a Recording **Destinations ⌘K** footer action, descriptor-backed searchable rows, click/keyboard activation routing, close/focus restoration, and exact action-ID-to-target round trips. The runtime matrix opens the actual popup, sees the seven current IDs, rejects `aichat`, activates Script Kit through real GPUI Enter dispatch, and observes exactly one target-generation increment with unchanged stop/delivery generations.
- **Chip decision:** Chips render only `frontmost`, `today`, `ask`, and `agentchat`. `chip_click_behavior` depends only on phase: Recording/Confirming retarget; all other phases ignore. Armed and Option inputs cannot change the result. Removed the global and local Option modifier monitor entirely. Footer primary copy derives from the selected descriptor (`Stop & <verb>` or direct `<verb>`).
- **Dismissal decision:** One exhaustive `OverlayEscapeAction` maps Recording→Confirming, Confirming→Resume, Transcribing/Delivering→Hide, Finished/Failed→Close, and Idle→Propagate. Local Escape, Command+W, global Escape, and native footer Close resolve and apply that same ladder. Explicit Backspace/Delete or Confirming Discard alone reaches abort. Hidden processing preserves the last/live state; the Dictation hotkey reveals it instead of starting/stopping another capture.
- **Escalations:** The first C11 integration pass exposed six stale `dictation_tab_ai_harness` source assertions. Current production had moved model availability to the selected-model catalog, empty capture to `handle_dictation_transcript`, return origin to the shared route owner, and detached chat policy to preserve the independent workspace. Repaired the existing tests structurally against current owners; did not change product behavior to satisfy obsolete strings. The focused suite's `default_model_path_lives_under_kit_path` races environment-mutating tests under parallel execution; the isolated test passes and the authoritative serial 236-test Dictation suite passes. The generated source inventory was two reader sites stale after C11 audit pruning; regenerated it at 2,814 sites and the main-base guard passes.
- **Focused proof:** Final Dictation model matrix 236/236 PASS with `--test-threads=1`; forced-route integration 8/8; lifecycle 7/7; overlay focus/hide 2/2; Agent Chat route 14/14. `check --lib`, `check --bin script-kit-gpui`, final stable build, and `git diff --check` PASS. Source inventory `--check --base main` PASS at 2,814 sites/no additions. Hardcoded-visual scanner 16/16 plus main-base guard PASS. Protected Bun contracts 40/40 and measured production calibration fixture 1/1 PASS.
- **Build/runtime:** `target-agent/artifacts/cons-flow-c11/script-kit-gpui`; SHA-256 `acbc504c1a46a2612d766456b85d070df7c366c479a716d24fbddd288d3fe857`. `.test-output/cons-flow-c11/dictation-dismiss-targets-receipt.json` passes `recording-confirm-resume-discard` and `processing-hide-reopen`. The first scenario enumerates and activates real Actions, selects all four quick chips, proves immediate Escape confirmation, Escape resume, Command+W confirmation, and explicit discard. The second proves four truthful disabled processing chips, hide without cancellation, and hotkey reopen.
- **Visual receipt:** `.test-output/cons-flow-c11/dictation-destinations-screen.png` captures the live overlay with four descriptor-backed chips and the Destinations footer action. `.test-output/cons-flow-c11/dictation-destinations-actions.png` captures popup chrome, but Tahoe targeted capture blacks the Metal list plane; the seven semantic rows plus real activation are the authoritative popup-content proof rather than a false visual claim.
- **Cleanup/privacy:** Both Driver rows report `processExited:true`, `streamsDrained:true`, and `logWriterClosed:true`; exact stable-artifact process inventory reports `exactArtifactOwnedProcessCount:0`. No microphone capture started, no transcript returned, no user config changed (before/after fingerprint equal), and no broad signal was used.
- **Governance:** No protected glass/material/motion value, fixture, threshold, or generated token changed. No new source reader or hardcoded visual call was added. Changes are confined to Dictation owners, three repaired Dictation contract tests, the generated source-audit inventory, the C11 probe, and required progress ledgers. `.hitl-align` and credentials were not inspected.
- **Adversarial audit:** PASS. Every enum variant resolves one descriptor; IDs are unique; legacy target cannot be selected or forged through Actions; all current targets appear in the real Actions host; four quick chips retain order; action/chip selection advances only target generation; stop/delivery/config remain unchanged; processing selectors are truthfully disabled; footer verbs match selected target; early Escape cannot abort; confirmation Escape resumes; processing hide cannot cancel; hidden hotkey reopens; only explicit Discard aborts; all four dismissal hosts share the ladder; exact artifact processes are gone; protected calibration is unchanged.
- **Decision branch:** The Oracle plan's “all current nonlegacy targets appear in Actions” was interpreted as a user-visible menu, not automation metadata. Current evidence showed only `targetActions` JSON, so the complete-product branch added and runtime-proved the actual Destinations Actions UI.
- **Lifecycle:** Local C11 commit completed. No push, deploy, tag, release, publication, credential access, or `.hitl-align` inspection performed.

### C12 — WF-020 frozen identity and WF-021 exactly-once delivery

- **Status:** Complete; local adversarial audit PASS; ready for atomic commit `fix(dictation-delivery): freeze identity and deliver exactly once [WF-020 WF-021]`.
- **Identity decision:** Added a closed frozen-destination model for Launcher, Prompt, Notes, existing/fresh Agent Chat, exact external app/window, captured Day Page, and fresh Quick AI. Identity is captured before recording concealment and on every retarget; every current descriptor requires it. Prompt generation uses GPUI entity identity or immutable inline definition plus the actual owning input value, never `AppView::Debug`. External capture uses native window ID and fails closed on ambiguity.
- **Coordinator decision:** History saves before delivery. One immutable transcript and one delivery ID enter the coordinator. `claim_dictation_delivery` atomically claims the ID before validation/mutation and retains failed/unknown IDs, preventing replay after a possible mutation. Internal errors and external validation failures emit typed stale receipts with zero destination attempts. No UI/current-frontmost fallback remains.
- **Target actors:** Launcher and Prompt compare exact generations before one owned input replacement. Notes compares instance/document/editor/anchor before one mutation. Today appends to the captured date/substrate. Existing Agent Chat verifies thread/generation; fresh Agent Chat creates Standard embedded; Quick AI creates fresh Quick AI with `NoContext`. Agent Chat and Quick AI record success only after the entry ticket reports accepted submission. External validates PID/bundle/window, activates and focuses that exact window, then performs one paste; an unknown paste result is `RetrySafety::Never`.
- **Escalations:** Initial Prompt identity hashed `AppView::Debug`, which cannot observe entity input edits; replaced it with owner values/entity IDs. The initial idempotency registry marked only after mutation; moved to an atomic pre-validation claim and extended it to external delivery. Initial Agent Chat/Quick AI receipts were written when dispatch was merely queued; threaded the must-use entry ticket into the coordinator and delayed success until accepted. The first Quick AI runtime rerun aborted because Dictation used `SuppressFocused`; corrected it to the host-required `NoContext`. Added a two-phase `freezeOnly`/`useFrozenSelection` DevTools seam after the initial runtime matrix could not induce real stale state between capture and delivery.
- **Focused proof:** Serial Dictation model suite 283/283 PASS. Forced routes 8/8, lifecycle 7/7, Agent Chat policy 14/14, and synthetic protocol 8/8 PASS. Dictation Agent Chat entry test PASS 1/1. `sk-protocol` 26/26 PASS. Binary check/build and formatting PASS.
- **Build/runtime:** `target-agent/artifacts/cons-flow-c12/script-kit-gpui`; SHA-256 `ad52a842b86b24c579a55fa24493f57f77ce11fb1aba6eecf3c2d45a338b7859`. `.test-output/cons-flow-c12/dictation-delivery-receipt.json` passes 11 isolated scenarios: Launcher, Prompt, Notes, captured Day Page, fresh Agent Chat, existing-thread Agent Chat, fresh Quick AI, unknown target, and changed Launcher/Prompt/Notes stale refusals. Every success records one attempt/one mutation and redacted identity/transcript facts; every stale row records zero attempts and no fallback.
- **Cleanup/privacy:** All 11 Drivers report `processExited:true`, `streamsDrained:true`, and `logWriterClosed:true`; exact artifact inventory is `exactArtifactOwnedProcessCount:0`. Microphone capture never starts. Receipts contain transcript lengths/fingerprints but no transcript text, external titles, paths, raw provider/OS errors, or credentials.
- **Governance:** Source inventory `--check` PASS at 2,814 reader sites with no new source reader. Hardcoded-visual tests PASS 16/16. `git diff --check` and formatting PASS. Protected glass Bun contracts PASS 40/40 and production calibration fixture PASS 1/1; no motion/material/geometry value changed. `.hitl-align` and credentials were not inspected.
- **Runtime boundary:** Automated positive/stale runtime proof covers every internal actor and both Agent Chat policies. Exact external paste/window-stale behavior remains model/integration-proved rather than mutating an uncontrolled user application; the product actor is fail-closed and was audited directly. Captured-date rollover is proved by the substrate behavior test rather than changing the machine clock or rebinding the user's real Brain substrate.
- **Adversarial audit:** PASS for product behavior. No current-view fallback or second destination exists; every target receives a frozen identity; badges do not query live external focus; prompt/entity input changes invalidate; Notes anchor/document changes invalidate; captured date survives rollover; legacy AI canonicalizes; Quick AI has no context; accepted submission gates chat receipts; delivery IDs are claimed before mutation; unknown paste cannot retry; receipts are redacted; exact artifact processes are gone. The only proof substitution is the controlled-runtime boundary above.
- **Decision branch:** A safe controlled external-app fixture was not present. The runtime matrix therefore does not paste into or close a user's arbitrary frontmost application; exact-window selection, ambiguity refusal, focus ordering, and no-fallback behavior are covered by focused model/integration contracts. This preserves user data while keeping the production actor strict.
- **Lifecycle:** Local C12 commit completed in this atomic commit. No push, deploy, tag, release, publication, credential access, or `.hitl-align` inspection performed.
