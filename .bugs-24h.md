# v0.1.18 bugs

- [x] Notes | QA 27 | Opening Actions crashed with a Root double-borrow. Shared Notes batch dispatch now uses the existing detached update helper. Same `design act --fixture notes.editor --action '{"type":"openActions"}'` passes in `.test-output/ship-24h/notes-actions-green/`; `notes-day-roundtrip` also passes after the fix. Owned processes exited.
- [x] Launcher | v0.1.18 retag | Owner-confirmed per-keystroke hashing: decoded icons and immutable script bodies now cache SHA-256 at creation; row publication consumes fixed-size digests. Binary check PASS (`build-515fcfb0-8fec-4f90-bc42-75dd3b28aa19`); clone/body-replacement and canonical row contracts PASS, 38 tests (`build-716f30c1-20ac-45ff-9552-22790147ce0e`). Both managed task receipts under `.test-output/managed-tasks/` report exit 0 and closed processes. Native held-Delete timing remains unmeasured.
- [x] Day Page | v0.1.18 retag | Saving a bound Notes document no longer repeats database bootstrap and full-corpus metadata backfill; the switcher still initializes storage before loading the binding. All 25 existing document persistence tests pass, including indexed save, failed-save dirty state, and external-edit preservation (`.test-output/managed-tasks/build-d8bfd547-c151-43f1-b815-9e7c59e4807f/task.json`, exit 0, closed). No native Cmd+S latency measurement claimed.
- [x] Notes ghost | v0.1.18 retag candidate | Bounded current-note, other-note, and clipboard candidate sources now persist across keystrokes; unchanged sources are compared without reparsing or cloning their candidates. Changed/removed sources and selected-note exclusion refresh from actual text, not timestamps. Ranking scans borrowed candidates without sorting the whole corpus. All 12 ghost behavior tests pass, including extended replacement/removal/selection and clipboard freshness checks (`build-b40e4217-5218-495d-8d5b-c2c1012af90d`, exit 0, closed). Native typing latency remains unmeasured.
- [x] Agent Chat image paste | v0.1.18 retag candidate | PNG encoding and temporary-file writes run off the UI thread without a base64 roundtrip. Tokens reserve their original caret/order before work starts; completion stays with the originating thread. Pending/failed images block draft submission through both UI and thread APIs, and canonical agent commands expose the same readiness. The headless regression passes with real PNG pixels, typing plus thread switching, failure/draft preservation, disabled Send, and token removal; all 26 focused Rust tests pass (`build-9bc5d24e-cd85-48ee-ba05-ac8f105f4803`, exit 0, closed). Native clipboard latency remains unmeasured.
- [x] Actions parent-key ownership | next v0.1.18 candidate | A focused real Actions InputState caused parent-forwarded typing to return unhandled; the regression failed at the parent-draft protection assertion (`build-557452bc-248e-4326-b8f7-ab99cfe45695`, closed). Explicit popup/parent origin now keeps forwarded typing, select-all replacement, and deletion in Actions while leaving popup-local edits to its native input. The headless regression and existing Escape/fixed-shell contracts pass: 45 passed, seven existing tests ignored (`build-8babc173-59aa-4680-971d-dcaa992f3b63`, exit 0, closed). No desktop input was used. The supervisor subsequently authorized shipping commit `2df922a4b` in v0.1.18.
- [x] Notes failed-save dismissal | next product commit | Before the fix, an immutable synthetic `fixture-alpha.md` left dirty=true and canonical content unchanged, yet real GPUI Escape closed Notes. SQLite retained the edited body: unsafe closure after a partial save, not destruction of every persisted copy. The same owned-runtime failure now keeps both Escape and Cmd-W open, with dirty text retained and no exit ticket. One shared error notification is visible in `notes-save-failure-green/save-failed.png` and exposed as a redacted semantic notification. Restoring the file's original flags and retrying Cmd-W persists the edited canonical Markdown and closes; content and flags were verified again after process exit. Evidence is under `.test-output/ship-24h/`; red: `notes-save-failure-immutable-file/`, green: `notes-save-failure-green/` (exit 0, closed). All 118 focused tests pass (`build-d5a47011-a4f7-4961-968b-385e4add7f18`); exact library+binary clippy passes (`build-ac10355c-c58e-47ca-8b96-d703c5815467`), both closed. Native traffic-light close remains a separate unverified path.
- [x] Notes custom frontmatter loss | next product commit | Big Notes finding #21 reproduced in the owned editor: saving owner, summary, and nested YAML silently removed all three from canonical Markdown despite dirty=false. Unknown fields now survive canonical save, visible reload, repeat edits, and index reconstruction; supplied values replace matching fields without allowing editor text to forge managed identity/timestamps. Existing canonical extras survive body-only index content. The regression includes nested reserved-looking keys, literal-scalar trailing newlines, and an indented `---`; 85 focused storage/metadata/substrate/Day Page tests pass (`build-445f84a1-1638-4761-ac00-871a007c1be5`). Exact lib/bin clippy passes (`build-c13da490-aa14-4cdc-b9e5-7c79a4e72b4a`). The same actual hidden-runtime input now preserves every field, then updates Alice to Bob; dirty=false follows the completed save, and canonical bytes remain identical after clean process exit. Red/green evidence: `.test-output/ship-24h/notes-frontmatter-repro/` and `notes-frontmatter-green/`; the GPUI frame shows the retained fields. No native input or new harness was used. YAML comments/formatting are not preserved; semantic values are.
- [x] Notes trash collision | post-v0.1.18 branch | Big Notes finding #15 reproduced: a new same-title note consumed another note's canonical trash file (`build-3d40be01-291f-466f-b8ec-1c028dd3ad07`, expected failing regression, closed). New/ordinary saves no longer restore trash. Only an indexed deleted-to-active transition restores, matching the document's note ID rather than a filename prefix; an occupied destination belonging to another note gets a fresh slug. The existing indexed slug lookup also supplies the transition state, avoiding an extra per-save query. All 46 storage tests pass (`build-2e2ccf28-01b9-4b21-a560-239328689a24`, exit 0, closed), including canonical trash byte preservation, repeated active save, original restoration without overwriting the replacement, and an unrelated plain day document left untouched. This is real database/filesystem behavior proof, not native UI proof.
- [x] Notes watcher overwrite | post-v0.1.18 branch | Big Notes finding #17 reproduced through the real watcher callback: its index update authorized a stale editor to overwrite an external capture without preserving any copy (`build-8a150085-fc55-41f4-b51f-91300f3b496d`, expected failure, closed). Indexing no longer advances the editor overwrite baseline. The same reproduction now preserves the external canonical document byte-for-byte in the existing conflict-copy path while saving the local edit; the watcher still updates the searchable index. All 47 storage tests pass (`build-6a39e5f6-a709-4c6f-800b-a7c51c26f072`, exit 0, closed). This proves database/filesystem behavior; it does not claim automatic editor merging or native watcher timing.
- [x] Notes missing day file | post-v0.1.18 branch | Big Notes finding #20 reproduced in the owned editor: deleting its synthetic day file stranded subsequent saves with dirty text and no canonical file (`.test-output/ship-24h/notes-missing-day-repro/`). Saving now atomically recreates a genuinely missing file; unsafe targets and other read errors still fail. `notes::window::owned_fixture_tests` reports **4 passed; 0 failed**, including the missing-file recovery regression, in `.test-output/managed-tasks/build-3056e788-9d7e-40ca-a185-c8fe3fc24261/task.json`. The supervisor explicitly accepted that test verdict: wrapper monitoring reported a transient incomplete sample during host swap exhaustion; test verdict unaffected. Overall wrapper exit remains 75 at `monitor-stop`, with complete within-budget pre/post censuses and closed cleanup. No repeat compiler, additional clippy run, or post-fix standalone application run was performed during memory exhaustion.
- [x] Actions resize while typing | POLISH.md P1 closed | The detached popup already freezes the root-unfiltered shell in `src/actions/window.rs:1605–1616`, derives its bounds at `1628–1644`, disables resizing at `1675`, and attaches the fixed shell at `1685–1686`; rendering fills that shell at `1182–1186`. `src/actions/dialog.rs:969–1005` owns the opening snapshot and fixed-height state; `4993` preserves that height during filtering. The existing owned-runtime `actions-filter-loop` receipt below also observed unchanged bounds across 11 → 0 → 1 → 11 rows. No sizing or calibrated motion values changed.
- Dictation decision: leave empty-caption Return and its caption-gated Stop affordance unchanged tonight. `src/dictation/tests.rs:5240–5258` explicitly locks the current behavior; whether empty-caption Return should stop capture is deferred to John's taste decision, not classified as a defect.
- [ ] File Search Actions Escape | unproven in hidden runtime; verify on desktop: launcher → Tab → Cmd+K → Escape; expected Actions closes, File Search stays | Hidden-focus differences are a hypothesis, not an established product defect. In `.test-output/ship-24h/gpui-propagation-runtime/`, attested binary `926e18e3733ce5d15e948991577b2e1f075aa444a2e4cbf715f3069e563bea7d` still returned FileSearchView → ScriptList on accepted request 11; `still-double-unwinds.png` shows the launcher. `actionsDialog.open` stayed true before AND after, so the earlier popup-close-first inference was unsupported. Renderer stops, a popup-aware root guard, and the authorized GPUI pre-callback checks did not repair this reproduction; every speculative source edit is reverted. Per the supervisor's first-red rule, defer and move on; remaining cross-surface/library checks were not run for the rejected candidate. Owned runtime ended with all windows closed and parent exit 0. No repair commit; v0.1.18 tag unchanged.
- [x] Theme immutable-file restore | no reproduction | Against an immutable synthetic `theme.json`, a real Down key previewed Dracula and changed the active theme (revision 2 → 5); Escape returned to ScriptList and restored all observed resolved values (revision 6). File bytes stayed unchanged while immutable, original file flags were restored, and owned runtime cleanup closed every window. Evidence: `.test-output/ship-24h/polish-reproductions-runtime/`. No restore-path or revision-reset change is justified by this case.
- Builtin return coverage: from the actual launcher, Clipboard History, File Search, Snap chooser, and Process Manager returned semantically to ScriptList on Escape. Microphone, Scratch Pad, Quick Terminal, and Claude did not open because owned device/storage/process guards refused their effects; their dismissal is unproven. Webcam opened its device-refused error view but ignored the targeted Escape; normal capture/focus behavior is unproven, and no stamp change was made. All checks were hidden; native visibility/focus was not observed.

## Verification gaps

- Big source review failed with `RESEARCH_PATH_DENIED` (`credential-content-whole-file`); no findings delivered and no paid retry started.
- Native hotkeys, desktop focus/input, clipboard delivery, microphone, and live AI remain excluded. Hidden runtime observations do not prove those stories.
- QA.md has no section H.
- QA A–D: no additional reproduced product failure. Native hotkey/launch/focus, microphone, clipboard delivery and provider-dependent steps skipped; fixture observations alone are partial, not PASS. Mini/Full geometry is fixed by the evaluator, not a proven app defect. Emoji batch input is unsupported. Auxiliary popup fixtures require parents.
- QA E–G: 22 terminal execution and 23 apply-back skipped; 25 action execution skipped; 26 chat opening observed but provider step skipped; 29 sigil filtering unproven; 30 Tab intentionally opens the cwd FileSearch picker, with selection/persistence skipped.
- Initial restricted runs confirmed no Dictation, Clipboard, or Agent Chat defect; they did not establish full coverage. The owner subsequently confirmed launcher per-keystroke body/icon hashing; focused contracts now pass.
- Adversarial gaps: native hotkey toggles, native held-key repeat timing, mid-animation Escape, and dead-port provider requests are excluded by the safe runtime. Only theme corruption was exercised, not all surface databases.
- 5 MB Notes input: transport send timed out before any application result. Owned stress process stopped; no remaining matching app process. Not an app PASS or product failure; no transport changes made.
- A standalone owned Actions `getLayoutInfo` loop failed with a redacted diagnostic. `request_closed` also occurs during successful loop cleanup, so it is not evidence of an application crash. State-only filtering passed; frame-qualified layout querying remains unverified. All owned resources closed; no frozen transport/probe changes were made.
- Further owned launcher QA observed `/r` → `/re` → Escape → `/` with command rows reset, then `@` with a CONTEXT header; `t` → `th` → `the` → `them` kept a leading RESULTS header. The loop stopped on a genuine `stale_target_identity` refusal during a later key request, not an application crash; cleanup closed all resources. These are partial same-frame observations, not an overall passing run or native held-key/flash-timing proof. The initial GPUI framebuffer is `.test-output/ship-24h/v019-initial-main.png`; native glass composition is excluded.
- Local Rust gate PASS: 15,336 passed, 0 failed, 45 ignored (`.test-output/managed-tasks/build-87d5f1ef-c972-4d20-b270-41a226090364/cargo.log`, closed task). Terminated the finished wrapper's leaked event-loop handle without changing tooling.
- Separate local gates passed: domain tests (184), first-run/permissions/mock-AI/privacy fixtures, consistency catalogue, SDK types/tests (261), Pi sidecar, and local bundle. The supervisor reports integration PASS; this session's earlier attempt was refused by a transient resource-monitor ENOENT. Safety guards remain unchanged. Local bundle is not the shipping artifact.

## Release handoff

- Release retry: CI run https://github.com/johnlindquist/script-kit-next/actions/runs/33604901156 completed with unsigned app/version/fmt/check/clippy/SDK gates green; Pi health, proof publication, and two Rust tests failed. No release was published. Pi health now passes from `env -i` with an empty HOME/cwd and the workflow's explicit synthetic metadata-only credential (36 models; no developer credentials or inference). Publication exclusively renames the staging directory before sealing its root; nine existing publication/failure tests pass locally, with macOS-14 proof left to CI. Codex completion observes channel EOF rather than child-exit timing; its test observes answer/completion and verified cleanup without orphan-helper timing. Window serialization observes worker identity and cross-PID rendezvous, not elapsed time. Both exact Rust filters PASS (`build-5a601e1a-9d36-485b-ae7f-9d15f721afdc`, exit 0, closed). Requested TypeScript files: 351 PASS / 3 resource-census timeouts with a 60s test timeout; the supervisor explicitly accepted that local gate and delegated authoritative proof to CI. Resource limits remain unchanged. Retag v0.1.18 from the current branch; if CI is not green by 04:00 MDT, stop release plumbing and resume Notes ghost/Chat paste.
- Replacement v0.1.18 tag points to `e5f7d1f047301760d74c31fac0c1298e0738524c`; current-branch push and tag creation succeeded. Historical tags were rejected by the broad `--tags` command and were not forced. Authoritative replacement run: https://github.com/johnlindquist/script-kit-next/actions/runs/33611650391.
- That run completed with unsigned app, Pi, format, check, clippy, and both SDK gates green. Rust had three Codex teardown failures; proof had eight failures (four blocked by one cold AlphaByte build timeout, four sealed-retirement renames) plus three setup/cleanup exceptions. Signing and publication were skipped.
- Next-retag repairs preserve EPERM diagnostics while bounded reaping verifies actual group disappearance; fixture shells reap their children. Retirement temporarily adds owner-write only to the identity-checked directory descriptor and restores its mode around quarantine rename; hardlinked files stay untouched. The proof job now caches its actual managed target even after failure and prewarms the exact AlphaByte unit/doc commands. Local proof: 26 focused Rust tests PASS (`build-9bc5d24e-cd85-48ee-ba05-ac8f105f4803`, exit 0, closed), six retention cases PASS, five workflow contracts PASS. No resource guard or frozen test timeout changed.
- Supervisor clarified **05:00 MDT**: stop starting new release-plumbing work, but finish the in-flight re-tag and leave its watcher running. The exact local gate is `./scripts/agentic/agent-cargo.sh clippy --locked --lib --bin script-kit-gpui --no-deps -- -D warnings` before commit/push. The earlier Actions exclusion is superseded by the linear-branch decision below.
- Third v0.1.18 candidate was `2016a4772e69bd90d4ac3257a5ad85fa7364cb16`, pushed with an exact old-tag lease alongside the branch. Bounded pre-push format/check passed and closed. Run: https://github.com/johnlindquist/script-kit-next/actions/runs/33615776629. Notes ghost and asynchronous image paste were included; signing and publication were skipped after the failures below.
- This run failed `Validate release clippy` on `expect_used` in `src/pasted_image.rs` and `let_unit_value` in the image completion callback. `Execute release Rust behavior` reported 15,336 passed, one failed, 45 ignored: `flows::run_registry::tests::multi_run_isolation_cancel_one_leaves_others` expected Cancelled but remained Running with invented process-group IDs. Reproduction commands are the exact clippy command above and `./scripts/agentic/agent-cargo.sh test --lib flows::run_registry::tests::multi_run_isolation_cancel_one_leaves_others`. Repairs remove both lint violations and invented fixture process identities without relaxing production cleanup checks; local verification passed as recorded below. All three Codex cleanup regressions passed this CI run.
- The same CI run passed all 1,911 proof tests / 9,374 assertions, then rejected Bun's CI-only suite `<properties>` metadata. Authorized parser repair accepts only an optional name/value-property block, preserving strict execution/count/assertion/nonpassing validation. Its with/without-metadata regression first failed only with metadata; all 13 JUnit tests now pass, and the production parser accepts the actual captured CI report (`.test-output/ship-24h/ci-third-bun-junit.log`) with unchanged counts. That historical report is not new-release proof.
- Release-only verification is now green: exact library+binary clippy (`build-0ee6b136-e0d8-4e48-ae2a-c8c548f7f9f1`) and 24 focused Rust tests (`build-44f8146a-1ce7-46a7-8e18-8b26f91596d2`), both exit 0 with closed resources. The preceding test invocation passed its 24 tests but was refused by an incomplete final resource census; the unchanged warm retry completed the guard successfully. No resource policy changed.
- Fourth v0.1.18 candidate was `689437f2710f2347af80d0b62ba5ede8b4a25fd4`, atomically pushed with the branch and an exact prior-tag lease. Bounded pre-push format/check passed. Run: https://github.com/johnlindquist/script-kit-next/actions/runs/33619611912; its integration failure blocked publication. Actions was initially kept out of that tag; inclusion is now authorized below.
- That run passed clippy and all 15,337 library tests (45 ignored), then integration compilation failed at `tests/script_content_model.rs:31`: `Option<String>` instead of `Option<ScriptBody>`. The same obsolete conversion was found in `tests/script_content_row_ui.rs:29`; both now use the existing `From<&str>` conversion. Local proof: all six integration targets passed, 55 tests (`build-5392faf5-72f2-48f0-baf4-c58e430c9188`); the additional row suite passed five tests (`build-45b61b20-b803-4d10-bae7-21f1ec36a4fa`); exact library+binary clippy passed (`build-99254a26-8262-4755-9fb9-96f699e4df75`). Every invocation exited 0 with closed resources. The supervisor explicitly authorized including Actions in v0.1.18 and re-tagging the branch tip: no detached release commit or history surgery.
- Fifth v0.1.18 candidate was `d03f93f057c6e56f077a7c3f070062b09f38d559`, including Actions and both cached-body integration fixtures. Atomic branch/tag push used the exact prior-tag lease; bounded pre-push checks passed. Run: https://github.com/johnlindquist/script-kit-next/actions/runs/33622503556. Its Rust behavior, unsigned app, version, format, check, clippy, SDK, and Pi jobs passed. All 1,913 proof tests / 9,396 assertions passed, then generated-byte parity rejected `tokens.json`; signing and publication were skipped.
- The supervisor explicitly authorized this post-cutoff exporter repair. A normal authoritative regeneration initially produced no diff. The Arg token path still consumed operator layout and host-native glass/theme state; it now uses the existing pure resolver with InfoBarBase, default layout, and the stock footer. Both generated outputs were regenerated, never hand-edited; actual Mini/Full heights are 312/356. Normal and empty-HOME exports are byte-identical (`sha256:a072c5b63993c7524dfa6e4f0f3cdf58f612cc86aa90d78c898c30f7b2276704`), with closed successful receipts `build-9ab3e421-7ae0-4335-8c3d-41f52ebfb0aa` and `build-8de5c9ae-ca93-4cb0-9d1f-0aa0ddc46996`. All 20 existing byte-comparison tests pass with the CLI timeout set to 60 seconds; no harness changes. Exact library+binary clippy passes (`build-50aa2e8f-c46d-41e8-b7c3-bfc18eb0ab04`, exit 0, closed). Production window geometry is unchanged.
- Sixth v0.1.18 candidate `b5d0557714c87a66e0b4292bc3c660c070537295` passed all validation gates, authoritative exporter JSON/CSS byte parity, and the unsigned macOS build. Run https://github.com/johnlindquist/script-kit-next/actions/runs/33626699617 then failed **Sign and notarize macOS app → Build and install the exact pre-sign compile artifact**: `process_timeout` after 1,804,980 ms (`build-6db1d2f2-6099-4652-8339-28bbc5d29172`, closed, no survivors). This was a cold compile timeout, not a signing-credential failure; publication was skipped.
- Seventh v0.1.18 candidate `c754117cb4d90e2f3062c0404493ad2bf8848364` passed its pre-sign compile after the authorized timeout/cache repair. Run https://github.com/johnlindquist/script-kit-next/actions/runs/33632728183 failed **Sign and notarize macOS app → Attest exact signing, accepted notarization, stapling, and Gatekeeper identities** (job `100266397032`): `signed artifact lacks hardened runtime`. The supervisor traced this to the parser incorrectly requiring a standalone `flags=` line; real codesign reports flags inside `CodeDirectory`. Signing, Apple acceptance, stapling, and Gatekeeper passed according to the supervisor's job-log investigation. A scoped parser/real-format fixture repair and eighth tag attempt are authorized; publication remains unproven.
- Authorized local census override committed as `0d72e83c0`: `SCRIPT_KIT_AGENT_INVENTORY_TIMEOUT_MS` accepts positive whole milliseconds through 300000, default unchanged at 8000. Existing inventory tests pass 42/42 at both default and the session's 90000 override; 18 direct boundary/invalid-environment checks pass. The README names the setting. Disk/worker limits and fail-closed incomplete-observation behavior remain unchanged.
- Eighth candidate `2e3d8fb6c10bd56afbdada61c611c87b3a072383` was atomically pushed as v0.1.18. The `CodeDirectory` repair and real-format fixtures passed 262 tests / 1498 assertions; bounded pre-push fmt/check passed. Run https://github.com/johnlindquist/script-kit-next/actions/runs/33640188169 failed **Sign and notarize macOS app → Build and install the exact pre-sign compile artifact** (job `100292428611`, exit 2): `process_timeout` after about 30 minutes. Resources stayed within limits; cleanup closed with no survivors. Signing, notarization, and publication were not reached.
  The supervisor identified the cause: build-ops overwrites the environment timeout with its own 30-minute default. The authorized workflow-only correction passes `--timeout-ms 5400000` explicitly; the next v0.1.18 tag may include Clipboard #15/#16. File Search's speculative repair remains reverted and deferred.
- Ninth candidate `4072d928daaf5686175d374b2e0aef6d607711f5` includes Clipboard preservation (`eb9743b97`) and the one-line workflow deadline correction. YAML parsing and shell syntax passed; bounded pre-push fmt/check passed with closed resources. The branch and v0.1.18 tag were atomically pushed with the exact eighth-tag lease. CI: https://github.com/johnlindquist/script-kit-next/actions/runs/33647828579 . Publication remains pending.
- Run 9 failed **Sign and notarize macOS app → Prove the exact signed packaged binary without showing a window**: the probe passed with six hidden-state assertions and closed managed cleanup, but the validator required retired safety/cleanup keys. With explicit authorization, only the packaged-root-frame validator and its existing fixture/negative cases now use the current sealed-evaluator fields and reject missing/unsafe flags, incomplete cleanup, survivors, and failure codes. All 262 release-evidence tests / 1,533 assertions pass. The real probe stdout is preserved at `.test-output/ship-24h/run-nine-packaged-root-frame.json`; the downloaded release-proof artifact contained only earlier proof receipts. Replaying that old receipt against the newer checkout correctly refuses the different source SHA; its provenance was not altered. The supervisor authorized the next v0.1.18 tag to include the intervening Clipboard, Dictation, and Notes fixes, superseding their original post-tag-only labels below.
- The next local app build stopped at `resource_observation_incomplete` (`build-602b8bb6-52c9-42ab-b760-a1a379a2893a`): no artifact published, closed cleanup, no survivors. Last measured target allocation was 42,535,907,328 / 42,949,672,960 bytes. Further builds are paused until the explicitly authorized retirement of this campaign's own obsolete outputs through the supported keep-set/prune interface; no shared-cache, pool, or unrelated deletion is authorized. Day Page production source remains unchanged and its next runtime reproduction has not started.

## Observed passing behavior

- Launcher: real GPUI typing `theme` selected Theme Designer; Escape dispatched; owned process exited.
- Launcher: 10,000-character input preserved (87 ms action); mixed emoji/CJK/RTL input preserved through real GPUI text dispatch.
- Notes/Day Page: existing `notes-day-roundtrip` passed 36 assertions including save/switch/reopen, undo, and canonical persisted content.
- Theme: existing `theme-publication-revert` passed 44 assertions including visible save errors and malformed reload preserving the last good theme.
- Actions: existing `actions-popup-activation` journey delivered the selected action; QA 28 observed Escape closing the popup.
- Current owned Actions filter observations: 11 → 0 → 1 → 11 rows, unchanged bounds `(402,80,340,402)`, fixed shell height 402, and fresh row geometry. Existing `design loop`, `.test-output/ship-24h/actions-filter-loop/`, exited 0; this measures hidden-runtime filtering, not native focus, key-repeat timing, or glass animation.
- Launcher: 50 real GPUI Down/Up events completed and returned selection to index 1; native repeat timing remains untested.
- Selected existing sentence schedules passed on the current owned binary: `sentence-find-notes/burst` (729 assertions) and `sentence-find-notes/correction-aba` (814 assertions), zero failures, closed resources. Outputs: `.test-output/ship-24h/v018-sentence-burst/` and `.test-output/ship-24h/v018-sentence-correction/`. Each aggregate CLI exited 3 because the unrequested remainder of the 224-schedule corpus was uncovered; these are selected-shard passes, not a whole-corpus PASS or native held-key timing proof.
- Notes: empty text accepted in 22 ms and observed persisted with no unsaved changes.
- SDK release baseline: `bun run scripts/test-runner.ts --parallel` passed 261/261 (three native/system-input files excluded by default).
- SDK inventory: current source already excludes supported `hotkey`, `mini`, and `micro`; all 18 current unsupported entries were traced to refusals/unimplemented routes. No inventory change needed.
- Fields SDK tests already assert supported ordered responses. Corrected only stale `test-fields-basic.ts` diagnostic prose; 14 focused Fields tests pass. Hidden `editable-prompt-validation` also proved invalid Fields input blocks submit, edited input is accepted, and completion occurs once; not native date-picker/styling proof or a new product fix.

## Big review findings (vega-alpha, 1-turn snapshot reviews, 2026-09-01 23:20)

Source-derived, NOT executed. Reproduce before fixing; CONFIRMED = reviewer traced the code path in the provided files. Ranked within each file by POLISH.md category order (latency, layout, Escape, crash, data loss, keyboard). Full text per surface:

- [ ] launcher | 26 findings (~15 CONFIRMED mentions, ~14 SUSPECTED) | `.test-output/ship-24h/big-review/launcher.md`
  - [ ] #2/#3 deferred without optimization: both character-array allocations and eager catalog clones remain source-derived findings. The owned replay accepted `@file:`/`@file:launch`; two seeded file rows eventually appeared, but the next clear event was refused before dispatch as `stale_target_identity`. No alias acceptance or complete slash/Delete replay is claimed. The sealed launch fixes `RUST_LOG=warn`, does not forward `SCRIPT_KIT_FILTER_PERF_LOG`, and provides no allocation/copy counters or scalable catalog control. Empty timing logs are not a zero-latency result. Evidence: `.test-output/ship-24h/launcher-allocation-replay/observations.json`; parent/app closed with no survivors. No new probe, hook, or source change.
- [ ] notes | 27 findings (~23 CONFIRMED mentions, ~7 SUSPECTED) | `.test-output/ship-24h/big-review/notes.md`
  - [x] #16 offline edits: two fresh invocations of the existing Rust test harness reproduced a cold-start save replacing an offline Markdown edit with stale SQLite content (`build-d2edb6d7-6bb5-4000-bb0c-0d3933f3092a`). Cold initialization now rebuilds the derived index from canonical files and seeds save-conflict baselines; already-initialized connections keep their existing behavior. All 48 Notes storage tests pass, including the same restart regression, cart retention, custom frontmatter, and watcher conflict protection (`build-013e6ef8-06e3-4aee-be7a-161b7d1df240`, exit 0, closed, no survivors). The regression uses private temporary data, not live notes or desktop input. This post-tag repair is for v0.1.19; v0.1.18 is not retagged.
- [ ] dictation_clipboard | 20 findings (~12 CONFIRMED mentions, ~12 SUSPECTED) | `.test-output/ship-24h/big-review/dictation_clipboard.md`
  - [x] #16 pinned oversize trimming: a real temporary SQLite database reproduced deletion of its pinned row (`build-103d2893-01e5-4f8f-b1aa-7f9c466e7397`). Size trimming now excludes pinned and brain-kept entries while still deleting unprotected oversized text. The same regression passes after the predicate fix.
  - [x] #15 failed image preparation: all image/path/URL/array preparation now precedes `clearContents` and `writeObjects`; owned image and path-string references are released on error paths. Per explicit authorization, proof is a unit publication-sequence check, not live clipboard reproduction: invalid PNG/UTF-8 paths never reach publication; valid input prepares both representations before the callback. Native write failure rollback is not claimed.
  - Both focused tests passed, wrapper exit 0, closed cleanup, no survivors: `.test-output/managed-tasks/build-95273707-9f5d-4d7b-9352-5ce005bc4ca7/task.json`. No system clipboard access or desktop input was used.
  - [x] #13 malformed legacy PNG: temporary SQLite metadata migration reproduced the byte-32 UTF-8 panic with `png:` + 31 ASCII characters + `é` (`build-7aa950e8-0bca-4bd8-931f-d43b84952f8a`). Header decoding now takes a checked byte slice; the corrupt row remains available without fabricated dimensions. Five focused tests passed, including valid/invalid headers and #15/#16 (`build-14d96706-e663-4e31-b3c8-57d6b7a57165`, exit 0, closed, no survivors). This post-tag fix is for v0.1.19; the ninth v0.1.18 tag is unchanged.
  - [x] #11 reused overlay: the existing owned controls reproduced recording generation 1→2 on the same window generation 2, with an empty new transcript but old caption pixels and a retained 314px frame. The production view now resets caption, scroll, waveform seed, and growth on a new recording identity. Green frames shrink 314→200px and clear the old words; empty/shorter partials within the same session retain the 314px height, and a third session renders fresh text at 200px. Evidence: `.test-output/ship-24h/dictation-reset-runtime/comparison.json` and adjacent PNGs; red frames are in `dictation-reuse-replay`. Both owned runs exited 0 with closed cleanup and no survivors. Seventeen existing Rust caption/state/calibration tests passed (`build-a3733b1c-ebfc-44ba-bb59-3d59b2374fde`), plus 46 existing Bun motion checks. Native exit-tail close/reopen and live glass probes were not exercised: desktop/device control remains excluded. This fix is post-tag for v0.1.19.
  - [ ] #18 deferred: the owned key event has no held/repeat field. Two discrete Escapes test intended recording/confirmation transitions, not held-key behavior; no native-input escalation or source change.
  - [ ] #20 deferred: owned controls do not populate the native pending-Escape flag. Local simulated keys cannot reproduce coalesced global monitor events; no source change.
  - [ ] #12 deferred: the existing microphone control pumps attachment work before returning and cannot pause in CreatedHidden/AttachPending. Normal open-popup dismissal does not prove the pending-lifetime case; no new hook or source change.
  - Next: remaining Notes and Agent Chat persistence findings while release CI runs. #17/#19 Return behavior stays parked for John; #9/#10 require visible-screen proof and are excluded.
- [ ] agent_chat | ? findings (~25 CONFIRMED mentions, ~11 SUSPECTED) | `.test-output/ship-24h/big-review/agent_chat.md`
- [ ] actions_components | ? findings (~11 CONFIRMED mentions, ~17 SUSPECTED) | `.test-output/ship-24h/big-review/actions_components.md`

Triage order: launcher latency #1–#3 and Delete-hold path; notes latency #1–#3 (save reruns DB init, ghost corpus rebuild per keystroke, Cmd+P corpus reread); agent_chat #1–#2 (clipboard image + skill discovery on foreground); actions_components Escape/resize items; dictation_clipboard caption-gated Return / repeated Escape. Everything data-loss-class gets reproduced even if SUSPECTED.
